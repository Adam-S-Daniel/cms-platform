---
title: Welcome Note
---
This is the site's own admin-editable content — the `notes` custom collection,
spliced in via `admin/collections.site.yml`. It is the ONLY collection /admin
shows on this single-page consumer; all five platform base collections are
hidden by `cms.base_collections: []`.
