# Reusable prod media-roundtrip loop for platform sites.
#
# A consuming site calls this from a thin wrapper that owns the schedule /
# push / workflow_dispatch triggers + run-name (see
# examples/site/.github/workflows/cms-media-roundtrip.yml). The e2e
# harness, the cms-recursion-gate + await-prod-deploy + post-failure-comment
# composites all live in the PLATFORM repo, so each job checks the platform
# out into `.cms-platform/` and runs the suite from there. Site identity
# (CMS_PROD_URL / CMS_APEX) + the PROD_PLAYGROUND_MODE sunset gate resolve
# to the CALLER's repo VARS under workflow_call.
#
# Runs the full Decap Media-UI lifecycle end-to-end against prod:
# upload a unique image via the Media UI → attach to a real _posts/
# entry → publish → assert it loads → remove from the post → publish →
# delete via the Media UI → assert the image URL 404s. See
# e2e/cms-media-roundtrip.spec.js for the rationale.
#
# PLATFORM PORT NOTES (vs adamdaniel.ai@origin/main): same shape as
# cms-publish-loop-prod.yml — site identity via repo VARS, GHCR ci-runner
# image + build-image job + container: block DROPPED in favour of inline
# deps, stuck-PR diagnostic DROPPED. The per-loop prod-mutating-loop-<key>
# lane + the cms-loop-lane-gate cross-loop serialization step (#70),
# await-prod-deploy gate, recursion-gate job, and the PROD_PLAYGROUND_MODE
# sunset gate are preserved.
#
# Sunset path: gated on the repo variable `PROD_PLAYGROUND_MODE` (see
# cms-publish-loop-prod.yml).
name: CMS Media Roundtrip — Prod (real upload + delete) (reusable)

on:
  workflow_call:
    inputs:
      platform_repo:
        description: "owner/name of the platform repo holding the e2e harness + composite actions."
        required: false
        type: string
        default: Adam-S-Daniel/cms-platform
      platform_ref:
        description: "Tag/sha of the platform repo to fetch the harness from (pin to the same ref as this workflow)."
        required: false
        type: string
        default: main
    secrets:
      CMS_E2E_PAT:
        required: false

permissions:
  # The spec drives admin via a fine-grained PAT (CMS_E2E_PAT); the
  # workflow itself only needs contents:read. `pull-requests: write` is
  # for the post-failure-comment composite. `actions: read` lets the
  # await-prod-deploy gate query this commit's deploy run.
  contents: read
  pull-requests: write
  actions: read

defaults:
  run:
    shell: bash

jobs:
  # Changed-files recursion gate. See cms-publish-loop-host.yml for the
  # full rationale.
  recursion-gate:
    runs-on: ubuntu-latest
    outputs:
      run: ${{ steps.gate.outputs.run }}
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.2 (2026-01-09)
        with:
          fetch-depth: 2

      - name: Checkout platform (e2e harness + composite actions)
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.2 (2026-01-09)
        with:
          repository: ${{ inputs.platform_repo }}
          ref: ${{ inputs.platform_ref }}
          path: .cms-platform

      - name: Recursion gate
        id: gate
        uses: ./.cms-platform/.github/actions/cms-recursion-gate
        with:
          loop: media

  media-roundtrip:
    needs: [recursion-gate]
    runs-on: ubuntu-latest
    concurrency:
      # PER-LOOP lane (#70). Each real-prod loop has its OWN concurrency
      # group, so a co-arriving SIBLING loop is NEVER evicted: a single push
      # salient to two loops used to land them both in one SHARED group where
      # GitHub keeps the running + latest-pending run and CANCELS the other
      # pending sibling (the cancel-in-progress:false mechanic behind #70 /
      # #1815). With a per-loop group GitHub keeps the LATEST run per group,
      # so a loop's most-recent run is never a spurious cancelled one.
      # cancel-in-progress:false still protects an in-flight SAME-loop run
      # (a kill mid-flow can leave the canary dirty); a duplicate same-loop
      # push just queues as the single pending run. Declared on the HEAVY
      # LOOP JOB, not the workflow, so the cheap recursion-gate job runs
      # OUTSIDE the lane (instant skip decision). Cross-loop MUTUAL EXCLUSION
      # is now enforced by the `cms-loop-lane-gate` step below (run-id
      # ordered, queue-not-evict). The group line differs across the three by
      # design (-prod / -media / -host); the SHARED invariant is the lane-gate
      # step, which IS drift-locked byte-identical — see
      # e2e/workflow-prod-loop-serialized.test.js.
      group: prod-mutating-loop-media
      cancel-in-progress: false
    env:
      # Site identity for the parameterized e2e helpers — supplied by
      # repo VARS so a consuming deployment passes its own deployed URLs.
      CMS_PROD_URL: ${{ vars.CMS_PROD_URL }}
      CMS_APEX: ${{ vars.CMS_APEX }}
      CMS_REPO: ${{ github.repository }}
    # Recursion guard delegated to the `recursion-gate` job above.
    if: ${{ needs.recursion-gate.outputs.run == 'true' }}
    # 150 min — accommodates the spec's 130-min TEST_TIMEOUT_MS (#1815: each of
    # the three URL-reflect legs now spans the full 30-min auto-merge latency)
    # plus checkout / npm ci / Playwright-install setup. The per-loop
    # concurrency lane + the cms-loop-lane-gate mean a stuck run blocks its
    # siblings (via the gate) only up to this bound, so the wait stays finite
    # and far cheaper than a deploy-lane collision.
    timeout-minutes: 150
    steps:
      - name: Checkout
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.2 (2026-01-09)

      - name: Checkout platform (e2e harness + composite actions)
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10  # v6.0.2 (2026-01-09)
        with:
          repository: ${{ inputs.platform_repo }}
          ref: ${{ inputs.platform_ref }}
          path: .cms-platform

      # Cross-loop serialization WITHOUT eviction (#70): wait for any
      # real-prod loop run that STARTED BEFORE this one (lower run id) to
      # finish before driving prod. Run-id ordering is a total order, so
      # co-arriving siblings queue in order instead of cancelling each
      # other. Fail-open + bounded. MUST stay byte-identical across the
      # three loops (drift-locked by workflow-prod-loop-serialized.test.js).
      - name: Loop lane gate — serialize real-prod loops without eviction (#70)
        uses: ./.cms-platform/.github/actions/cms-loop-lane-gate

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e  # v6.4.0 (2026-04-20)
        with:
          node-version: "20"

      - name: Install harness deps
        working-directory: .cms-platform/e2e
        run: npm ci

      - name: Install Playwright browser + system deps
        working-directory: .cms-platform/e2e
        run: npx playwright install --with-deps chromium

      # Post-merge only: do NOT drive prod until THIS commit is live.
      # Skipped on workflow_dispatch/schedule and when playground mode is
      # off (the spec self-skips, so there is nothing to gate).
      - name: Await prod deploy of this merge
        if: ${{ github.event_name == 'push' && vars.PROD_PLAYGROUND_MODE == 'true' }}
        uses: ./.cms-platform/.github/actions/await-prod-deploy
        with:
          prod-url: ${{ vars.CMS_PROD_URL }}

      - name: Skip — playground mode disabled
        if: vars.PROD_PLAYGROUND_MODE != 'true'
        run: echo "::notice::PROD_PLAYGROUND_MODE is not 'true'; skipping media round-trip."

      - name: Run media round-trip spec
        if: vars.PROD_PLAYGROUND_MODE == 'true'
        working-directory: .cms-platform/e2e
        env:
          # SITE_ROOT — the CONSUMING site's checkout (github.workspace). The e2e
          # harness runs from .cms-platform/e2e here, so the base_collections guards'
          # default (resolve __dirname/..) would point at the PLATFORM, not the
          # consumer; set it so keepsBaseCollection() reads the consumer's _config.yml
          # and a single-page bio (base_collections:[]) correctly SKIPS the collection
          # specs instead of timing out on a Posts sidebar link that never renders.
          SITE_ROOT: ${{ github.workspace }}
          CMS_E2E_PAT: ${{ secrets.CMS_E2E_PAT }}
          TARGET: prod
          RUN_PROD_MUTATE_PLAYGROUND: "1"
        run: |
          set -o pipefail
          npx playwright test \
            cms-media-roundtrip.spec.js \
            --project=chromium-desktop-3k \
            --reporter=list \
            --max-failures=1 \
            --workers=1 \
            2>&1 | tee /tmp/media-roundtrip.log

      - name: Upload test results on failure
        if: ${{ (failure() || cancelled()) && vars.PROD_PLAYGROUND_MODE == 'true' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a  # v7.0.1 (2026-04-10)
        with:
          name: media-roundtrip-results
          path: |
            .cms-platform/e2e/test-results/
            /tmp/media-roundtrip.log
          retention-days: 14

      - name: Post failure summary
        if: ${{ failure() && vars.PROD_PLAYGROUND_MODE == 'true' }}
        uses: ./.cms-platform/.github/actions/post-failure-comment
        with:
          mode: post
          log-file: /tmp/media-roundtrip.log
          marker: media-roundtrip-failure-summary
          title: media-roundtrip

      - name: Resolve failure summary on success
        if: ${{ success() && vars.PROD_PLAYGROUND_MODE == 'true' }}
        uses: ./.cms-platform/.github/actions/post-failure-comment
        with:
          mode: resolve
          marker: media-roundtrip-failure-summary
          title: media-roundtrip

      # #22: delete this loop's EPHEMERAL canary branches on completion AND
      # on cancel/failure. The media-roundtrip spec force-pushes a per-run
      # cms/posts/2099-12-31-e2e-media-roundtrip-<runId> branch via Decap; a
      # cycle that cancels/fails mid-flight orphans it (~35 piled up on
      # adamdaniel). The spec's runId is Date.now(), so the workflow can't
      # know the exact branch — pattern-delete every branch on this loop's
      # OWN prefix that has NO open PR (a live cycle's branch always has its
      # in-flight cms/* PR). Idempotent + FAIL-OPEN: every step here is
      # `|| echo`-guarded and the step is continue-on-error, so a cleanup
      # hiccup (API blip, missing PAT on a fork) NEVER fails the loop. Uses
      # CMS_E2E_PAT (contents r/w) since the workflow only grants
      # contents:read. Locked by e2e/workflow-loop-branch-cleanup.test.js.
      - name: Clean up ephemeral media-roundtrip canary branches
        if: always()
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.CMS_E2E_PAT }}
          GH_REPO: ${{ github.repository }}
          BRANCH_PREFIX: cms/posts/2099-12-31-e2e-media-roundtrip-
        run: |
          set -uo pipefail
          if [ -z "${GH_TOKEN:-}" ]; then
            echo "::notice::no CMS_E2E_PAT — skipping ephemeral branch cleanup (fail-open)"
            exit 0
          fi
          echo "::notice::Cleanup — deleting orphaned ${BRANCH_PREFIX}* branches with no open PR"
          gh api --paginate "repos/${GH_REPO}/branches?per_page=100" \
            --jq ".[] | select(.name | startswith(\"${BRANCH_PREFIX}\")) | .name" \
            2>/dev/null | while read -r branch; do
            [ -z "$branch" ] && continue
            # Belt-and-braces prefix re-verify: never touch a branch outside
            # this loop's own ephemeral prefix.
            case "$branch" in
              "${BRANCH_PREFIX}"*) : ;;
              *) echo "skip ${branch} — prefix mismatch"; continue ;;
            esac
            open_count=$(gh pr list --state open --search "head:${branch}" --limit 1 --json number --jq 'length' 2>/dev/null || echo "1")
            if [ "$open_count" != "0" ]; then
              echo "skip ${branch} — has open PR(s) (live cycle)"
              continue
            fi
            echo "delete ${branch}"
            gh api -X DELETE "repos/${GH_REPO}/git/refs/heads/${branch}" \
              || echo "::warning::failed to delete ${branch} (already gone?) — ignoring (fail-open)"
          done
          echo "::notice::Cleanup complete (fail-open)"
